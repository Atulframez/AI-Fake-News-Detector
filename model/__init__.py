# AI Fake News Detector — Model Package (ensemble v2)

# Modules: detector, preprocessor, scraper

# Pipeline: raw text → preprocess → TF-IDF → ensemble → verdict

# Version bump: v1.1.0 — session counters, expanded patterns, scraper helpers
__version__ = '1.2.0'  # added credibility_ratio, confidence_label, verdict_emoji, batch helpers
